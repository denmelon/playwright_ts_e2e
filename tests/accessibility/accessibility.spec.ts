// This file will contain accessibility tests
// Accessibility tests check if your app is usable by everyone
// Example:
// import { test } from '@playwright/test';
// import { injectAxe, checkA11y } from 'axe-playwright';
// test('homepage is accessible', async ({ page }) => {
//   await page.goto('/');
//   await injectAxe(page);
//   await checkA11y(page);
// });

// Add accessibility checks for key pages
